#include "main.h"
#include <stdio.h>

/**
 * mul - multiplies 2 numbers
 * @a: number to be mutiplied
 * @b: number to be multiplied by
 *
 * Return: the product of a and b
 */
int mul(int a, int b)
{
	int pdt;

	pdt = a * b;
	return (pdt);
}
