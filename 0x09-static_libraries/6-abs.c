#include "main.h"

/**
 * _abs - calculates the absolute value of a number
 * @n: number whose absolte value is to be determined
 *
 * Return: the calculated absolute value
 */

int _abs(int n)
{
	return (n);
}
