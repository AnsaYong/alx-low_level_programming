#include <stdio.h>

/**
 * main - code that prints all single digits of base 10
 *
 * Return: 0
 *
 */
int main(void)
{
	int a = 0;

	while (a < 10)
	{
		printf("%d", a);
		a++;
	}
	printf("\n");
	return (0);
}
